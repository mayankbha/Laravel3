/*$('.variable-width-big').on('beforeChange', function(event, slick, currentSlide, nextSlide){
	var pageCurr = $("#page-carousel").val();
	var totalCurr = $("#total-carousel").val();
	console.log(totalCurr + " curr index " + nextSlide);
	if(totalCurr == (nextSlide+1))
	{
		
	}
});*/
